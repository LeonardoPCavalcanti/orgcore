{
  "name": "4med-prototipo",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "banco": "docker compose up -d postgres"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "eslint": "^9.17.0",
    "typescript-eslint": "^8.19.0"
  }
}
