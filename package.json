{
  "name": "4med-prototipo",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "test": "pnpm run typecheck && pnpm run lint && pnpm -r test",
    "lint": "eslint .",
    "typecheck": "pnpm -r --if-present run typecheck",
    "banco": "docker compose up -d postgres",
    "dev": "pnpm -r --parallel --if-present run dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "eslint": "^9.17.0",
    "typescript-eslint": "^8.19.0"
  }
}
